---
  - name: Setup Bastion Host
    hosts: localhost
    connection: local
    gather_facts: false
   
    tasks:
      - name: import vpc setup variables
        include_vars:
          file: vpc_setup.txt
      
      - name: import vpc output variables
        include_vars:
          file: vpc_setup.txt

    
